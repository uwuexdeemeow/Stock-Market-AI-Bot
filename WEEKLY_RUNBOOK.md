# Operations Runbook — see `Documentation/WEEKLY_RUNBOOK.md`

This file used to carry its own copy of the operations runbook; the
copies drifted, which led to "which one is current?" confusion.  The
canonical runbook now lives in one place:

→ **[Documentation/WEEKLY_RUNBOOK.md](Documentation/WEEKLY_RUNBOOK.md)**

That file has:

- a goal-indexed quick reference
- cadence map (daily / weekly / monthly / quarterly)
- a script catalog with one-line descriptions
- key-files reference
- git-sync commands
- common-failure fixes
- safety + real-capital-blocker checklists

Edit the canonical version only — this stub just points there.
