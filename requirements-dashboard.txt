# Dashboard-only dependencies.  Install with:
#   pip install -r requirements-dashboard.txt
#
# These are additive on top of your main requirements.txt — the
# dashboard reuses pandas, numpy, etc.  from the main install.

streamlit>=1.30,<2.0
plotly>=5.18,<6.0
