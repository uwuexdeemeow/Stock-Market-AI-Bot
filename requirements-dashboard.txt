# Dashboard-only dependencies.  Install with:
#   pip install -r requirements-dashboard.txt
#
# These are additive on top of your main requirements.txt — the
# dashboard reuses pandas, numpy, etc.  from the main install.

streamlit>=1.30,<2.0
plotly>=5.18,<6.0
# Needed by the GitHub Actions page so the SSL cert chain works on
# macOS/Windows Python installs that don't ship the system CA store.
# Also used as a fallback when `requests` isn't available.
certifi>=2024.0.0
# Preferred HTTP client for the GitHub API call — handles SSL natively
# via certifi.  Falls back to urllib + certifi if missing.
requests>=2.31,<3.0
