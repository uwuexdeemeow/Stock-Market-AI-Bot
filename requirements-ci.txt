# Slim requirements for GitHub Actions CI daily run.
# Excludes torch, transformers, moomoo (not needed for Alpaca-only daily pipeline).
# This keeps CI install time under 60 seconds and avoids the 2 GB PyTorch wheel.

# Core data & ML
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
xgboost==3.2.0
scipy==1.17.1
joblib==1.5.3

# Market data
yfinance==0.2.40
finnhub-python==2.4.27

# NLP / Sentiment (used by research.py for factor features)
vaderSentiment==3.3.2
finvader==1.0.2
nltk==3.6.2

# Data I/O
pyarrow==23.0.1
feedparser==6.0.12
beautifulsoup4==4.14.3
requests==2.33.1

# Broker (Alpaca only)
alpaca-trade-api==3.2.0

# Calendar (NYSE holiday detection)
exchange_calendars==4.13.2

# Utilities
python-dotenv==1.2.2
tqdm==4.67.3
psutil==7.2.2
rich==15.0.0
python-dateutil==2.9.0.post0
pytz==2026.1.post1
