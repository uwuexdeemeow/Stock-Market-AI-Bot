# Handoff Note — 2026-09-26

A new Claude Code session (for example, a cloud session at claude.ai/code)
should read this file first. It picks up the work started on the project
computer on 2026-09-26.

## Current state (already done and pushed)

- **Audit and cleanup:** `Documentation/CODEBASE_AUDIT_2026-09-26.md`.
- **Daily run was safety-blocked** by the one-day-delay stress test. The
  owner chose to paper trade with a warning while researching a replacement
  strategy. See `Documentation/DELAY_STRESS_PAPER_ADVISORY.md` (read it
  before touching any gate). The paper release was re-frozen at `7ee19ca`.
- **Delay-aware walk-forward selection** was added (`676dd22`). Its flag is
  `--selection-entry-delay-days 1`.
- **Colab run `wf_delay_robust_lowturnover_20260926`: finished and
  REJECTED.** All 10 folds were valid, but the unchanged approval rules said
  no: selector alpha correlation −0.531 (must be > 0), and selector Sharpe
  uplift −0.258 (it did worse than the frozen incumbent). Result files are in
  `Colab/result_20260926/signals/`. The rejection is recorded in
  `DELAY_STRESS_PAPER_ADVISORY.md`.
- **Timing-luck diagnostic (done on the project computer):** the incumbent
  was run from each of the 20 possible rebalance start days. Its 2023–2026
  alpha vs QQQ swings from −72 to +74 points by start day alone, while a
  one-day delay costs only about 2 points on average. The failing delay
  stress row is mostly timing luck.
- **Hypothesis H-tranche: pre-registered, then REJECTED.** A preview of 4
  staggered slices cut the spread to 24.6 points (under the 73 limit), but 3
  of 10 books lost to QQQ over 2023–2026. With the luck averaged out, the
  incumbent's recent edge is about +3 points and not reliable. Tranche
  support will **not** be added to the locked engine. Scripts and raw output
  are in `research_evidence/phase_luck_20260926/`.
- **Incumbent stays on the paper advisory.** The **owner** will choose the
  next signal hypothesis. Don't pick one or run a new grid on your own.
- **Colab can now run the research scripts:** the notebook has
  `RUN_PHASE_LUCK` (off by default), and the snapshot packs
  `signals/core_satellite_alpha_metrics.json`. Results go to
  `stockbot_phase_luck_result.tar.gz` in Drive.
- **Branch:** this work is on `worktree-phase-luck-research`. Merge it into
  `main` before making a Colab snapshot, because Colab checks out the
  snapshot's commit.
- **Colab snapshot fix** (`552b4b1`): snapshots now include every file the
  research manifest fingerprints. The cause was Windows CRLF line endings
  versus LF in a Git clone.

## Hard rules

1. **Never** loosen approval thresholds or safety gates, bypass the daily
   guard, place orders, publish a live config, or approve real capital,
   unless the owner explicitly asks for that specific change.
2. **Locked files:** `paper_version_lock.json` lists them (for example
   `alpaca_paper_trading.py`, `paper_health.py`, `core_satellite_alpha.py`,
   `robustness_review.py`, `validation_bundle.py`, the workflows). Editing
   one makes `python paper_validation_epoch.py --check-lock` fail, and daily
   paper trading then stops until the release is re-frozen. Ask the owner
   before editing a locked file. Re-freeze only after tests pass, the change
   is committed and pushed, and the owner has agreed:
   `python paper_validation_epoch.py --freeze-current`, then commit
   `paper_version_lock.json`. The freeze needs a clean working tree.
3. **Research rules** fixed in `DELAY_STRESS_PAPER_ADVISORY.md`:
   - A replacement must pass every execution-stress scenario outright, with
     no paper advisory.
   - Don't widen the grid in response to results.
   - Record rejections.
4. **Project instructions** (`CLAUDE.md`): comment code for a beginner, keep
   one doc per script in `Documentation/`, and reply in the owner's
   preferred caveman style.
5. **Local-only data:** `data/`, `models/`, `.env` and `keys.txt` are **not**
   in Git. A cloud session can change code and run tests (the tests use fake
   data), but it can't run real backtests or stress tests.
6. Run `python -m pytest -q` before every commit. The last full run was 840
   passed, 9 skipped.

## Open tasks (owner's priority order)

### 1. DONE — Stress-test a research candidate without publishing it

Done in the cloud follow-up pass (see the audit file). Use
`--candidate-json PATH [--candidate-name NAME]` on both scripts.

Original task text:

`core_satellite_execution_stress.py` (`_selected_config`, line ~28) and
`core_satellite_survivorship_audit.py` (line ~34) can only test the approved
live config, through `validation_bundle.load_approved_research_config`. The
Colab winner therefore can't be stress-tested without publishing it, and
publishing is forbidden. Add a `--candidate-json PATH` option to both that:

- loads a candidate config, either a walk-forward result's
  `approved_live_config.config` or a plain config JSON;
- writes to separate research outputs (for example
  `logs/research_candidate_execution_stress_<name>.json`) and **never**
  overwrites `logs/core_satellite_execution_stress.json`,
  `logs/core_satellite_survivorship_audit.json` or their `signals/` CSVs;
- stamps the payload `"research_candidate": true, "approves_trading": false`;
- leaves default behavior (no flag) byte-for-byte unchanged.

Add tests with fake panels or mocks, plus documentation in
`doc_core_satellite_execution_stress.md` and
`doc_core_satellite_survivorship_audit.md`. Neither script is locked.

### 2. DONE — Two small bugs in LOCKED files (fixed and re-frozen)

- `alpaca_paper_trading.py` `snapshot_equity` (line ~2946) stamps the date
  with `datetime.now()`. On UTC runners after 8 PM New York time, today's
  equity is labeled as tomorrow. Fix:
  `now = datetime.now(EXECUTION_TIMEZONE).replace(tzinfo=None)`.
- `paper_health.py` line ~1131: `years = live_days / 252.0` should be
  `(live_days - 1) / 252.0`, because N snapshots cover N-1 return days.

### 3. DONE — Stale stress result in the signal file (display now uses today's reports)

`core_satellite_alpha.py` line ~2811 embeds `execution_stress_review` when
the signal is written. The daily workflow refreshes the stress report later,
so the embedded value is the previous day's. The block `reason` field is
correct. The fix is cosmetic; ask the owner first.

### 4. DONE — Cleanup (see the audit file's follow-up section)

- `tests/test_strategies.py` lines 344–547: four `@pytest.mark.skip` legacy
  classes call functions that no longer exist. Delete them; the owner has
  approved cleanup.
- `predict.py` line ~612 computes a calibrated `p_up` that is never used.
  Either use it or remove the calibrator loading. Explain the choice to the
  owner.

## Next step (waiting on the owner)

**H-edge has been run: edge shown.** On a point-in-time S&P 500 list the
incumbent keeps 78% of its 2013–2022 alpha and beats all 100 random-pick
runs. Both live trailing stops (8% on stocks, 5% on SPY/QQQ) cut alpha far
more than they cut drawdown, so they should go. The H1 fix (paper trades on
the tested 20-day calendar, no trailing stops) is on branch
`h1-paper-calendar` for owner review. See the H-edge results in
`DELAY_STRESS_PAPER_ADVISORY.md`.

**H-bakeoff has been run: no winner.** Ideas A, B and C all failed the
pre-registered gates, and the incumbent stays on the paper advisory. See
"Result: H-bakeoff" in `DELAY_STRESS_PAPER_ADVISORY.md`, including the lesson
about scaling the spread limit to the window length.

The owner will choose the next signal hypothesis: a new signal, or a refresh
of the current one for the post-2022 market. When they do:

1. Write the hypothesis and its pass/fail rule into
   `DELAY_STRESS_PAPER_ADVISORY.md` and commit it **before** any run.
2. A cloud session may write the research code and tests. Real runs happen
   on the project computer or in Colab.
3. Judge the result only by the rule written in step 1, and record it
   either way.

## When a Colab walk-forward result comes back

1. Unpack `stockbot_colab_result.tar.gz` on the project computer, which has
   `data/`.
2. Read the approval block and the fold `on_time_score` / `delayed_score`
   values in the JSON.
3. If a config was selected, run:
   `python core_satellite_execution_stress.py --candidate-json <result.json>`
   and `python core_satellite_survivorship_audit.py --candidate-json <result.json>`.
4. Replace the incumbent only if everything passes outright **and** the
   owner agrees. Otherwise record the rejection in
   `DELAY_STRESS_PAPER_ADVISORY.md` and keep the incumbent on the paper
   advisory.
