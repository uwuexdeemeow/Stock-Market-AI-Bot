# order_accounting.py

## Evidence closure update

stage_attempt_counts counts unique accepted a1/a2 broker children submitted inside an explicit interval. Complete-fill and any-fill rates differ for partial orders. Duplicate identities, missing timestamps and invalid quantities prevent a complete denominator. This library is used by the existing execution-report workflow; it has no order submission interface.
